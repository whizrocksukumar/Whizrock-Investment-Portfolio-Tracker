
/**
 * Gemini Service (Disabled)
 * All AI analysis features have been disconnected to ensure zero costs.
 */

export const generatePortfolioAnalysis = async (): Promise<string> => {
  return "AI Analysis is currently disabled to prevent usage charges.";
};

export const generateStockAnalysis = async (): Promise<string> => {
  return "AI Intelligence features are disconnected.";
};
